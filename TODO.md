# TODO - Implement Functional Selection Mode

## Steps
- [x] 1. Add `tags-cell` class to the Tags `<td>` in `displayDashboard()` in `script.js` so "Select by Tag" and tag search filtering work correctly.
- [x] 2. Verify no other selection-related gaps remain (global state, select files, dynamic column, selection counter, cancel).
- [ ] 3. Reload dashboard and manually verify features.
